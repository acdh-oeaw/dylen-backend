type Query {
    allAvailableCorpora: [Corpus]
    networkById(id: ID): EgoNetwork
    networkByTargetWord(target_word: String): [EgoNetwork]
}

type Corpus {
    id: ID
    name: String
    sources: [Source]
}

type Source {
    id: ID
    name: String
    networks: [EgoNetwork]
}

type EgoNetwork {
    id: ID
    targetWord: String
    year: Int
    corpus: ID
    source: ID
    absFreq: Int
    relFreq: Float
    threshold: Float
    nodes:[Node]
    connections:[Connection]
}

type Node {
    id: ID
    clusterId: Int
    text: String
    pos: String
    similarity: Float
    absFreq: Int
    relFreq: Float
}

type Connection {
    id: ID
    node1: ID
    node2: ID
    similarity: Float
}